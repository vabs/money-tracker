# Money Tracker Configuration
# Copy this file to .env and customize the values

# NOTE: Initial configuration is now in public/transactions.json
# These environment variables are kept for backward compatibility
# but transactions.json is the recommended way to configure the app

# Initial amount in dollars (fallback if transactions.json is not found)
VITE_INITIAL_AMOUNT=1000

# Annual interest rate as a percentage (fallback if transactions.json is not found)
VITE_ANNUAL_INTEREST_RATE=7

# Start date in YYYY-MM-DD format (fallback if transactions.json is not found)
VITE_START_DATE=2024-01-01

# Base path for GitHub Pages (e.g., /money-tracker for username.github.io/money-tracker)
# Leave empty or set to / for custom domain or username.github.io deployment
VITE_BASE_PATH=/
